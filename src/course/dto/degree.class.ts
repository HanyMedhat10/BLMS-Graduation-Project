export class DegreeClass {
  title: string;
  degree: number;
  student: number;

  constructor(title: string, degree: number, student: number) {
    this.title = title;
    this.degree = degree;
    this.student = student;
  }
}
